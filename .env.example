# Copy to .env and fill in. NOT needed to run anything here:
# the whole repo works offline with zero credentials.
GUARDRAIL_MODE=mock

# --- Production swap (optional) ---
# Point these at your real provider when replacing a regex stage with an
# LLM-as-judge stage. Nothing in this repo reads them today.
# LLM_PROVIDER_API_KEY=
# MODERATION_API_KEY=
